---
title: "pos_stripe_tap"
description: "Adds Stripe Tap to Pay (Android M2) terminal support to the Point of Sale via a custom controller and an OWL patch on ProductScreen."
icon: "package"

# Provenance
generated_by: "Engine:Scribe"
generation_date: "2026-08-02"
last_reviewed_date: "2026-08-05"
reviewed_by: "Engine:Continuity"

# Overall confidence
confidence: "medium"

# Per-section confidence
confidence_map:
  connection_map: "high"
  dependencies: "high"
  cross_module_connections: "medium"
  models: "high"
  views: "high"
  business_logic: "medium"
  reports: "high"
  controllers: "high"
  security: "medium"
  static_assets: "medium"
  troubleshooting: "medium"
  native_odoo_19_comparison: "low"
  continuity_review: "medium"
  recommendations: "medium"
---

<Warning>
  This page was generated by `Engine:Scribe` on **2026-08-02** and has not been
  human-reviewed. Overall confidence: **medium**. Verify low-confidence sections
  before production use.
</Warning>

# POS Stripe Tap to Pay

`pos_stripe_tap` registers a new POS payment terminal type called **Stripe Tap (Android M2)** and wires it to the native Stripe Terminal SDK through an Android POS JavaScript bridge. It adds a small Python model override, one JSON controller for Stripe connection tokens, and a single OWL patch on the `ProductScreen`.

## Connection Map

<CardGroup>
  <Card title="Models" icon="database">
    Extends `pos.payment.method` to add the `stripe_tap` terminal selection.
  </Card>
  <Card title="Views / Actions" icon="layout">
    No XML views, actions, or menu records. UI changes are entirely in OWL/JS.
  </Card>
  <Card title="Security" icon="shield">
    No new groups, access rights, or record rules. Inherits POS security model and relies on `auth="user"` on the controller route.
  </Card>
  <Card title="Controllers" icon="globe">
    One JSON controller: `/pos_stripe_tap/connection_token` creates a Stripe Terminal `ConnectionToken`.
  </Card>
  <Card title="Reports" icon="file-text">
    None.
  </Card>
  <Card title="Assets" icon="code">
    Patches `ProductScreen` in the `point_of_sale._assets_pos` bundle.
  </Card>
</CardGroup>

## Dependencies

<AccordionGroup>
  <Accordion title="Direct manifest dependencies">
    - `point_of_sale` — Required for `pos.payment.method`, the `ProductScreen` component, and the `point_of_sale._assets_pos` bundle.
  </Accordion>
  <Accordion title="Soft / implied dependencies">
    - `stripe` Python package — Declared in `external_dependencies.python`. Required by the connection-token controller at runtime.
    - `base` / `ir.config_parameter` — The controller reads `stripe.secret_key` from system parameters.
    - `web` — The OWL patch imports `@web/core/utils/patch` and `@point_of_sale/app/screens/product_screen/product_screen`.
    - `AndroidPos` runtime bridge — The JS helper `stripeTapPay` expects a global `window.AndroidPos` object exposed by the Android POS wrapper. Not a declared Odoo dependency.
  </Accordion>
</AccordionGroup>

## Cross-Module Connections

<Warning>
  Confidence: **medium** — connections inferred from static analysis; runtime wiring may differ.
</Warning>

### Inherits / extends from other custom modules

| Model | Extends | Source module | Source file | Notes |
|-------|---------|---------------|-------------|-------|
| *None* | — | — | — | `pos_stripe_tap` only extends native Odoo `point_of_sale` models and components. |

### Other modules extend this

| Model | Extended by | Source module | Source file | Notes |
|-------|-------------|---------------|-------------|-------|
| *None* | — | — | — | No custom module declares a dependency on `pos_stripe_tap` or references its records. |

### Shared mixins / abstract models

| Mixin | Used by | Notes |
|-------|---------|-------|
| *None* | — | No abstract models or mixins are shared with `pos_stripe_tap`. |

### Shared XML IDs referenced

| XML ID | Referenced in module | Defined in module | Usage |
|--------|----------------------|-------------------|-------|
| *None* | — | — | The module has no `data/` files and does not reference XML IDs defined in other custom modules. |

### Inter-module method calls

| Caller module | Caller method | Calls | Target module | Notes |
|---------------|---------------|-------|---------------|-------|
| Shared asset bundle | `point_of_sale._assets_pos` | Patches from `header_replacer`, `pos_qol_overrides`, `mobile_pos_relayout`, `pos_stripe_tap` | `point_of_sale` | All four custom modules add files to the same POS frontend bundle. `pos_qol_overrides` also patches `ProductScreen` (`getNumpadButtons`, `payCash`), so load order and patch composition are relevant. |
| `pos_qol_overrides` | `ActionpadWidget` template (`props.fastValidate`) | `ProductScreen.fastValidate` → patched in `pos_stripe_tap` | `pos_stripe_tap` | When a fast-pay payment method has `use_payment_terminal = "stripe_tap"`, the `pos_qol_overrides` card button routes into the `stripe_fast_validate.js` patch. Indirect, runtime-only coupling; confidence **low**. |

### Orphaned / unresolved references

| Reference | Expected target | Why it is flagged |
|-----------|---------------|-------------------|
| *None* | — | No unresolved references were found. |

## Models

| Model | Type | Source file | Notes |
|-------|------|-------------|-------|
| `pos.payment.method` | Inherited | `models/pos_payment_method.py` | Adds `stripe_tap` to the terminal-selection list. |

### Inherited methods

#### `pos.payment.method._get_payment_terminal_selection()`

**File:** `models/pos_payment_method.py`

Extends the base method by appending one tuple:

```python
("stripe_tap", "Stripe Tap (Android M2)")
```

This makes the new terminal type available in the POS payment-method form.

## Views

No XML views, inherited templates, actions, or menus are defined in this module. The only UI layer is the OWL patch in `static/src/app/screens/product_screen/stripe_fast_validate.js`.

## Business Logic

### Registering the terminal type

<Steps>
  <Step title="Payment method form loads terminal choices">
    Odoo calls `pos.payment.method._get_payment_terminal_selection()` to populate the **Use a Payment Terminal** field.
  </Step>
  <Step title="Stripe Tap appears as an option">
    The override appends `("stripe_tap", "Stripe Tap (Android M2)")` to the native list.
  </Step>
</Steps>

### Processing a Stripe Tap payment

<Steps>
  <Step title="Cashier validates a Stripe Tap order">
    `ProductScreen.fastValidate` is invoked when the configured payment method uses a terminal.
  </Step>
  <Step title="OWL patch intercepts the flow">
    If `paymentMethod.use_payment_terminal != "stripe_tap"`, the patch delegates to `super.fastValidate(...)` and does nothing.
  </Step>
  <Step title="Build payment parameters">
    For a Stripe Tap order, it reads:
    - `amountCents` = `Math.round(order.totalDue * 100)`
    - `currency` = lowercase `this.pos.currency.name` (defaults to `"usd"`)
    - `orderRef` = `order.pos_reference || order.uid`
  </Step>
  <Step title="Call Android POS bridge">
    `stripeTapPay` checks for `window.AndroidPos`. If missing, it rejects with an error. Otherwise it sets a one-time `window.onStripePaymentResult` callback and calls `AndroidPos.payWithStripe(amountCents, currency, orderRef)`.
  </Step>
  <Step title="Resolve or reject">
    The Android wrapper invokes `window.onStripePaymentResult(success, paymentIntentId, error)`. On success, the promise resolves with the Stripe `paymentIntentId`; on failure, it rejects with the provided error message.
  </Step>
  <Step title="Complete native validation">
    If `stripeTapPay` succeeds, the patch calls `super.fastValidate(paymentMethod)` to finalize the order in Odoo. If it fails, a danger notification is shown and the validation aborts.
  </Step>
</Steps>

### Connection-token endpoint

<Steps>
  <Step title="Client requests a token">
    The Android POS app or a frontend client POSTs to `/pos_stripe_tap/connection_token`.
  </Step>
  <Step title="Controller reads Stripe key">
    `StripeTapController.connection_token` fetches `stripe.secret_key` from `ir.config_parameter` via `sudo`.
  </Step>
  <Step title="Stripe Terminal token created">
    It calls `stripe.terminal.ConnectionToken.create()` and returns `{ "secret": token.secret }`.
  </Step>
</Steps>

<Warning>
  The controller uses `sudo()` to read the system parameter. Any authenticated Odoo user who can reach the route can trigger a Stripe connection-token creation. Confidence: **medium**.
</Warning>

## Reports & QWeb

No reports, QWeb templates, or paperformats are defined.

## Controllers & Routes

| Route | Type | Auth | Method | Returns | File |
|-------|------|------|--------|---------|------|
| `/pos_stripe_tap/connection_token` | `json` | `user` | `POST`-style JSON | `{ "secret": <stripe_token_secret> }` | `controllers/main.py` |

### Payload & response

The route accepts no required parameters. It returns a JSON object with the Stripe Terminal connection-token secret:

```json
{
  "secret": "pstest_..."
}
```

<Warning>
  Name collision: the `ir.config_parameter` key `stripe.secret_key` is not namespaced to this module. If other Stripe integrations use the same key, the value will be shared and changing it may affect all of them. Confidence: **medium**.
</Warning>

## Security

| Element | Definition | Notes |
|---------|------------|-------|
| Groups | None | No new groups or implied IDs. |
| Access rights | None | No `ir.model.access.csv` or explicit access records. |
| Record rules | None | None declared. |
| Route auth | `auth="user"` | The connection-token route requires an authenticated Odoo session. |
| Config read | `ir.config_parameter.sudo()` | `stripe.secret_key` is read with elevated privileges. |

## Static Assets / OWL

| Bundle | Files | Purpose |
|--------|-------|---------|
| `point_of_sale._assets_pos` | `static/src/app/screens/product_screen/stripe_fast_validate.js` | Patches `ProductScreen` and adds the Stripe Tap payment helper. |

### OWL Components

| Component | Source | Notes |
|-----------|--------|-------|
| `ProductScreen` (patched prototype) | `static/src/app/screens/product_screen/stripe_fast_validate.js` | Adds `fastValidate(paymentMethod)` override. |

### Helper function

- `stripeTapPay(amountCents, currency, orderRef)` wraps the Android `AndroidPos.payWithStripe` call in a Promise and installs a temporary `window.onStripePaymentResult` callback.

## Troubleshooting

| Symptom | Likely cause | Check |
|---------|--------------|-------|
| `stripe_tap` not in the terminal dropdown | `pos_stripe_tap` not installed, or another module overrides `_get_payment_terminal_selection` without calling `super()`. | Verify module is installed and the model override is loaded. |
| `ModuleNotFoundError: stripe` | The `stripe` Python package is not installed in the environment. | Install `pip install stripe` and restart the Odoo worker. |
| Connection-token route returns 500 | `stripe.secret_key` system parameter is missing or invalid. | Check `Settings > Technical > System Parameters` for `stripe.secret_key`. |
| "Stripe Tap is only available in the Android POS app" | The frontend is running in a desktop browser or a non-Android wrapper. | Confirm `window.AndroidPos` is injected by the Android host. |
| Card payment fails with no clear error | `AndroidPos.payWithStripe` rejected without an error message, or the callback returned `success=false`. | Inspect the Android wrapper logs and Stripe Terminal reader status. |
| `fastValidate` not intercepting | `paymentMethod.use_payment_terminal` is not `stripe_tap`. | Verify the payment method's terminal field is set to **Stripe Tap (Android M2)**. |
| Payment succeeds but order is not validated | `super.fastValidate` was not reached after the bridge call. | Ensure `stripeTapPay` resolves and no unhandled exception occurs before the `super` call. |

## Native Odoo 19 Comparison

### `pos.payment.method._get_payment_terminal_selection`

**Native Odoo 19:** The base implementation returns the list of supported payment terminals, e.g., `stripe`, `adyen`, `six`, etc., depending on installed modules.

**This module:** Appends `("stripe_tap", "Stripe Tap (Android M2)")` to the list.

**Why it matters:** Odoo 19 ships a native `pos_stripe` integration that uses Stripe readers through a different frontend/backend architecture. This module adds a parallel terminal type that uses a custom Android bridge rather than the native Stripe Terminal flow.

### `ProductScreen.fastValidate`

**Native Odoo 19:** `fastValidate` initiates the configured payment terminal flow (e.g., Stripe, Adyen) through the native terminal abstraction and completes the order.

**This module:** For `stripe_tap` only, it intercepts validation, converts the order total to cents, and delegates card capture to `window.AndroidPos.payWithStripe`. Only after the bridge reports success does it fall through to the native `fastValidate` to finalize the order.

**Why it matters:** The custom flow bypasses the native terminal abstraction and relies on a non-standard global JS bridge. It also changes the currency handling: if `this.pos.currency.name` is missing, the helper defaults to `"usd"`, which may not match the company's actual currency.

### Stripe Terminal connection-token flow

**Native Odoo 19:** The `pos_stripe` module provides its own backend controllers, payment-method model, and frontend service for discovering Stripe readers and creating payment intents.

**This module:** Implements a single minimal controller (`/pos_stripe_tap/connection_token`) that creates a Stripe Terminal `ConnectionToken` from a system parameter. Reader discovery, payment-intent creation, and capture happen inside the Android wrapper, not Odoo.

**Why it matters:** The integration is intentionally thin on the Odoo side. Most of the Stripe Terminal lifecycle is handled by the Android host, which limits visibility for Odoo developers when a payment fails or needs to be reconciled.

<Warning>
  Confidence for the native Odoo 19 comparisons is **low** because the exact behavior of the native `pos_stripe` module and the Android POS wrapper were inferred from naming and comments, not verified at runtime.
</Warning>

## See Also

- [`pos_qol_overrides`](/modules/pos_qol_overrides) — its action-pad and
  ProductScreen templates call `fastValidate`, which is intercepted by this
  module when the selected payment method is `stripe_tap`.
- [`mobile_pos_relayout`](/modules/mobile_pos_relayout) — shares the
  `point_of_sale._assets_pos` bundle and patches the same `ProductScreen`
  template area.
- [`header_replacer`](/modules/header_replacer) — shares the
  `point_of_sale._assets_pos` bundle for its POS event registration popup patch.
- [`mam_setup`](/modules/mam_setup) — install-all meta module that does **not**
  currently list this module as a dependency; this module must be installed
  independently or added to `mam_setup`'s manifest.

## Recommendations

<Warning>
  Confidence: **medium** — these are opinions, not verified fixes. Review before acting.
</Warning>

### High impact, low effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 1 | Add `pos_stripe_tap` to the `mam_setup` manifest `depends`. | `mam_setup` is the install-all meta module for the MAM stack, but it omits this module. Installers using the single-click setup will not get Stripe Tap support unless they manually install it. | One-line manifest change; no code or database impact. | high |
| 2 | Namespace the `ir.config_parameter` key to `pos_stripe_tap.secret_key`. | The current key `stripe.secret_key` is generic and can collide with other Stripe integrations (e.g., a future eCommerce Stripe module). | Requires a one-time reconfiguration or migration of the existing system parameter; avoids silent key sharing. | high |
| 3 | Add a missing-key guard in `StripeTapController.connection_token`. | If `stripe.secret_key` is missing, the Stripe SDK call raises an opaque 500. Returning a clear JSON error speeds up ops diagnosis. | Small code addition; must keep the route contract stable. | medium |

### High impact, high effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 4 | Evaluate migrating to the native Odoo 19 `pos_stripe` integration. | Odoo 19 ships `pos_stripe` with reader discovery, payment-intent tracking, and built-in reconciliation. The custom Android bridge reimplements parts of that lifecycle outside Odoo. | Likely requires different hardware/firmware or Android wrapper changes; may remove the need for this module entirely. | medium |
| 5 | Move payment-intent creation and capture into Odoo instead of the Android wrapper. | Today reader discovery, intent creation, and capture happen inside the Android host, so Odoo has limited visibility into failures and cannot reconcile payments directly. | Requires new controllers, backend state tracking, and Android wrapper changes; significantly expands the module's scope. | low |

### Low impact, low effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 6 | Restrict the connection-token route to a POS manager group. | The route is currently `auth="user"`, so any authenticated Odoo user can create a Stripe Terminal connection token. | Must verify that kiosk/session users do not need direct token access; can be combined with a proxy group if needed. | medium |
| 7 | Document the `window.AndroidPos` contract and payment-result callback. | The bridge protocol is implicit in the code. A short contract doc reduces risk when the Android wrapper or this patch are updated independently. | Documentation-only; does not change runtime behavior. | low |

### Odoo-native alternatives

| Custom pattern | Native Odoo 19 equivalent | Confidence |
|----------------|---------------------------|------------|
| Custom `stripe_tap` terminal type and `window.AndroidPos` bridge | Native `pos_stripe` module and its Stripe Terminal reader/service abstraction | medium |
| Manual `ConnectionToken` controller | `pos_stripe` backend controllers and frontend Stripe service | medium |

## Agent Review Log

<AccordionGroup>
  <Accordion title="2026-08-02 — Engine:Scribe">
    - Initial documentation generated from module source.
    - Documented one inherited model (`pos.payment.method`) and one controller route (`/pos_stripe_tap/connection_token`).
    - Documented the OWL patch on `ProductScreen` and the `AndroidPos` JS bridge dependency.
    - Flagged the shared `stripe.secret_key` system parameter and the `sudo()` read as security notes.
    - No views, reports, security records, or static assets beyond the single JS patch were found.
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Cartographer">
    - Added **Cross-Module Connections** section.
    - Confirmed `pos_stripe_tap` is a leaf module: no custom module depends on it and it does not inherit from any custom module.
    - Documented shared `point_of_sale._assets_pos` bundle usage with `header_replacer`, `pos_qol_overrides`, and `mobile_pos_relayout`.
    - Flagged indirect runtime coupling: `pos_qol_overrides` ActionpadWidget templates call `fastValidate`, which can route to the `pos_stripe_tap` `stripe_tap` patch if that terminal type is selected.
    - Verified no shared XML IDs, no model name collisions, and no orphaned references across the custom module set.
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Continuity">
    - Added `continuity_review: "medium"` to `confidence_map`.
    - Added a `See Also` section with cross-references to `pos_qol_overrides`, `mobile_pos_relayout`, `header_replacer`, and `mam_setup`.
    - Reciprocal cross-link to the `pos_qol_overrides` page, which documents the `fastValidate` coupling from the action-pad Card button.
    - Noted that `mam_setup` does not currently declare this module as a dependency.
    - Updated `last_reviewed_date` and `reviewed_by` to reflect this continuity pass.
    - Kept overall confidence at **medium** (static analysis only, untested at runtime).
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Advisor">
    - Added the `Recommendations` section and set `confidence_map.recommendations: "medium"`.
    - Recommended adding `pos_stripe_tap` to `mam_setup` manifest dependencies and namespacing the `stripe.secret_key` system parameter.
    - Flagged the custom Android bridge as a candidate for native `pos_stripe` evaluation, with medium confidence due to the amount of wrapper behavior outside Odoo.
    - Suggested a missing-key guard in the connection-token controller and a more restrictive group on the route.
    - Documented an Odoo-native alternative table comparing the custom terminal type/bridge to `pos_stripe`.
  </Accordion>
</AccordionGroup>
