---
title: "pos_qol_overrides"
description: "Small POS quality-of-life overrides for event ticketing, anonymous registrations, and mobile register UX."
icon: "package"

# Provenance
generated_by: "Engine:Scribe"
generation_date: "2026-08-30"
last_reviewed_date: "2026-08-09"
reviewed_by: "Engine:Continuity"

# Overall confidence
confidence: "medium"

# Per-section confidence
confidence_map:
  connection_map: "high"
  dependencies: "high"
  cross_module_connections: "medium"
  models: "high"
  views: "high"
  business_logic: "high"
  security: "high"
  troubleshooting: "medium"
  continuity_review: "medium"
  recommendations: "medium"
---

<Warning>
  This page was generated by `Engine:Scribe` on **2026-08-10** and has not been
  human-reviewed. Overall confidence: **medium**. Verify low-confidence sections
  before production use.
</Warning>

# Connection Map

<CardGroup>
  <Card title="Models" icon="database">
    Extends `pos.order`, `event.registration`, `pos.config`, `res.config.settings`, and `event.mail`. Introduces `pos.anonymous.override` and `pos.ticket.email.batch`.
  </Card>
  <Card title="Views & Actions" icon="layout">
    Backend `ir.actions.act_window` and menu for `pos.anonymous.override`.
  </Card>
  <Card title="Security" icon="shield">
    `ir.model.access.csv` grants user access to anonymous overrides and system access to ticket batches.
  </Card>
  <Card title="POS Frontend" icon="monitor">
    Patches `PosStore`, `ProductScreen`, `PaymentScreen`, `ReceiptScreen`, `PartnerList`, `Orderline`, `Navbar`, `ActionpadWidget`, and several event popups. Adds `EventLookupPopup`, `EventAttendeeListPopup`, ticketbooth barcode scanner result, and login screen card reader re-pair.
  </Card>
  <Card title="Customer Display" icon="users">
    Extends `CustomerDisplay` and `CustomerDisplayPosAdapter` to show member-pricelist discounts.
  </Card>
  <Card title="Scheduled Jobs" icon="clock">
    `ir.cron.pos_ticket_email_batches` processes pending `pos.ticket.email.batch` records every 5 minutes.
  </Card>
  <Card title="Integrations" icon="plug">
    Enqueues Blackbaud membership sync jobs via `mam_integration_hub` when membership products are sold in POS.
  </Card>
  <Card title="Settings" icon="settings">
    `pos.config.x_ticketbooth_mode` enables barcode ticketbooth check-in per register; exposed in `res.config.settings`.
  </Card>
  <Card title="Controllers" icon="globe">
    Public receipt page at `/pos/receipt/<order_id>` with token-gated access.
  </Card>
</CardGroup>

# Dependencies

<AccordionGroup>
  <Accordion title="Direct manifest dependencies">
    - `point_of_sale` — required for all POS screens, models, and services.
    - `pos_event` — required for event registration popups, event tickets, and `event.registration` integration.
    - `pos_stripe_tap` — required for `stripe_tap_ensure_portal_access()` used in membership portal SMS flow.
  </Accordion>
  <Accordion title="Soft / implied dependencies">
    - `event` — used for `event.registration`, `event.event.ticket`, `event.question`, and the `event.action_report_event_registration_full_page_ticket` report.
    - `mail` — used for `mail.template` and `mail.mail`.
    - `base` — standard `res.users`, `ir.cron`, and `ir.attachment` usage.
    - `mam_integration_hub` — optional; `pos.order._enqueue_blackbaud_memberships()` calls `mam.blackbaud.source.enqueue_membership_sync` when the model is available.
    - `mailchimp_x_odoo_txsms` — optional; SMS sent by `_ensure_membership_portal_sms()` is rerouted to Mandrill when this module is installed.
  </Accordion>
</AccordionGroup>

## Cross-Module Connections

<Warning>
  Confidence: **medium** — connections inferred from static analysis of manifests
  and source trees; runtime wiring and patch load order may differ.
</Warning>

### Inherits / extends from other custom modules

| Model | Extends | Source module | Source file | Notes |
|-------|---------|---------------|-------------|-------|
| _None found_ | — | — | — | `pos_qol_overrides` only inherits native/base models (`pos.order`, `event.registration`) and native `pos_event` OWL components. It does not extend models or components introduced by other custom modules. |

### Other modules extend this

| Model | Extended by | Source module | Source file | Notes |
|-------|-------------|---------------|-------------|-------|
| _None found_ | — | — | — | No other custom module inherits `pos.anonymous.override`, `pos.ticket.email.batch`, or the `pos.order`/`event.registration` overrides introduced here. |

### Shared mixins / abstract models

| Mixin | Used by | Notes |
|-------|---------|-------|
| _None found_ | — | No abstract models or mixins are defined or consumed across this module boundary. |

### Shared XML IDs referenced

| XML ID | Referenced in module | Defined in module | Usage |
|--------|----------------------|-------------------|-------|
| `point_of_sale.menu_point_rep` | `pos_qol_overrides` | `point_of_sale` (native) | Parent menu for `menu_pos_anonymous_override`. |
| `event.action_report_event_registration_full_page_ticket` | `pos_qol_overrides` | `event` (native) | Rendered in `pos.ticket.email.batch._send_ticket_email()` to generate ticket PDFs. |
| _Internal to pos_qol_overrides_ | — | — | `email_template_pos_event_order_confirmation`, `ir_cron_pos_ticket_email_batches`, `action_pos_anonymous_override`, `menu_pos_anonymous_override` are defined and consumed only inside this module. |

### Inter-module method calls

| Caller module | Caller method | Calls | Target module | Notes |
|---------------|---------------|-------|---------------|-------|
| `pos_qol_overrides` | `pos.order._enqueue_blackbaud_memberships()` | `mam.blackbaud.source.enqueue_membership_sync(...)` | `mam_integration_hub` | <Info>High confidence: source read directly.</Info> Called from `action_pos_order_paid()` only when the `mam.blackbaud.source` model exists and the order contains products under the "Membership" category. |
| `pos_qol_overrides` | `pos.order._ensure_membership_portal_sms()` | `pos.payment.method.stripe_tap_ensure_portal_access(partner)` | `pos_stripe_tap` | <Info>High confidence: source read directly.</Info> Called for membership orders; early-returns if portal user already exists. SMS delivery rerouted to Mandrill when `mailchimp_x_odoo_txsms` is installed. |
| _None found_ | — | — | — | No direct Python or OWL calls between `pos_qol_overrides` and other custom modules. RPC methods (`action_mark_registrations_present`, `lookup_registrations_by_reference`) and `pos.ticket.email.batch` methods operate on native models only. |

### Shared POS asset bundles and component collision risks

| Bundle | Module | Files | Components touched | Notes |
|--------|--------|-------|--------------------|-------|
| `point_of_sale._assets_pos` | `pos_qol_overrides` | Multiple JS/SCSS/XML files | `PosStore`, `OrderSummary`, `ProductScreen`, `PaymentScreen`, `ActionpadWidget`, `Navbar`, `EventConfiguratorPopup`, `EventRegistrationPopup`, `PartnerList`, `PartnerLine`, `Orderline`, `ReceiptScreen` | See collision notes below. |
| `point_of_sale._assets_pos` | `header_replacer` | `static/src/app/components/event_registration_popup/event_registration_popup.js` | `EventRegistrationPopup` | Autofills name/email/phone from the selected order partner. |
| `point_of_sale._assets_pos` | `mobile_pos_relayout` | `mobile_pos_relayout.js`, `mobile_pos_relayout.xml` | `Navbar`, `ProductScreen`, `ActionpadWidget` | Adds mobile pane switching, replaces review button with Customer button, hides `SelectPartnerButton` on small screens, adds New Order menu item. |
| `point_of_sale._assets_pos` | `pos_stripe_tap` | `stripe_fast_validate.js` | `ProductScreen` | Adds `fastValidate` for Stripe Tap payment terminal. |
| `point_of_sale.customer_display_assets` | `pos_qol_overrides` | `customer_display.xml`, `customer_display_adapter.js` | `CustomerDisplay`, `CustomerDisplayPosAdapter` | Mirrors member-pricelist discount display. |

<Warning>
  Component collision — `EventRegistrationPopup` is patched by both
  `pos_qol_overrides` and `header_replacer`. Both mutate the same `state`
  fields (`byRegistration`, `byOrder`). `header_replacer` runs during `setup()`
  and prefills answers from the partner; `pos_qol_overrides` later blanks answers
  when an anonymous override is signed. Load order depends on manifest/asset
  order and may affect the final prefilled values. Verify combined behavior
  before editing either patch.
</Warning>

<Warning>
  Component collision — `Navbar`, `ProductScreen`, and `ActionpadWidget` are
  patched by both `pos_qol_overrides` and `mobile_pos_relayout`. `mobile_pos_relayout`
  changes the register button behavior, adds a Cart button, and hides the partner
  button on small screens; `pos_qol_overrides` adds Card/Cash switchpane buttons,
  a "View Payment Screen" dropdown item, and a "Reference Lookup" dropdown item.
  XPath targets are different, but both modules assume the same base template
  structure. Odoo patch order is not guaranteed; test on mobile after any
  update to either module.
</Warning>

<Warning>
  Name collision / shared concern — `pos_qol_overrides` displays a member
  pricelist discount (`pricelistDiscount`) on the `Orderline` and customer
  display. The actual pricelist assignment is not managed by this module; it is
  provided by `mam_membership_loyalty` (or any other module that sets the
  partner's `specific_property_product_pricelist`). If `mam_membership_loyalty`
  is not installed, the discount line will only appear when a standard
  pricelist yields a lower price.
</Warning>

<Warning>
  Soft dependency — `receipt_screen.js` reads `event_slot_id.start_datetime` on
  `event.registration` to decide whether to auto-mark attendance within ±12 hours.
  The `event.slot` model and the `event_slot_id` field are **not** defined by this
  module or by any other custom module in the MAM repository. They must be
  supplied by a companion module (commonly co-installed with
  [`website_event_calendar_slots`](/modules/website_event_calendar_slots)). If that
  provider is missing, the auto-mark present logic falls back to
  `event_id.date_begin`. Verify combined behavior when calendar slots are enabled.
</Warning>

### Orphaned / unresolved references

| Reference | Expected target | Why it is flagged |
|-----------|-----------------|-------------------|
| _None found_ | — | All model references and XML IDs resolve to modules declared in this module's manifest (`point_of_sale`, `pos_event`, `event`, `mail`, `base`) or the optional `mam_integration_hub` Blackbaud hook. |

# Models

| Model | Type | Source file | Key fields / methods |
|-------|------|-------------|----------------------|
| `pos.anonymous.override` | New | `models/pos_anonymous_override.py` | `order_id` (Many2one `pos.order`), `order_uuid`, `session_id`, `user_id`, `signed_by_name`, `category` (`museum_employee` / `requests_anonymity`), `reason` (Text, required) |
| `pos.ticket.email.batch` | New | `models/pos_ticket_email_batch.py` | `order_id` (required), `registration_ids` (Many2many `event.registration`), `email_to`, `part_number`, `part_count`, `state` (`pending` / `processing` / `done` / `error`), `error_message`, `sent_date` |
| `event.registration` | Inherited | `models/event_registration.py` | `_update_mail_schedulers()` — skips per-registration mail scheduler for POS-sourced registrations; `lookup_attendees_by_event()` — door list grouped by identity; `action_pos_ticketbooth_scan()` — barcode scan + mark present |
| `pos.order` | Inherited | `models/pos_order.py` | `action_pos_order_paid()`, `send_paid_order_mail()`, `action_mark_registrations_present()`, `lookup_registrations_by_reference()`, `action_mark_group_present()`, `action_unmark_group_present()`, `lookup_tickets_by_partner()`, `_enqueue_blackbaud_memberships()`, `_ensure_membership_portal_sms()`, `_ensure_access_token()` |
| `pos.config` | Inherited | `models/pos_config.py` | `x_ticketbooth_mode` — per-register ticketbooth check-in mode |
| `res.config.settings` | Inherited | `models/res_config_settings.py` | `pos_x_ticketbooth_mode` — related field exposing `x_ticketbooth_mode` in settings |
| `event.mail` | Inherited | `models/event_mail.py` | `_send_mail()` — suppresses POS-linked registrations from core event mail scheduler |

<Info>
  Confidence: **high**. All models, fields, and methods are directly readable from the seven Python files.
</Info>

# Views

<Tabs>
  <Tab title="Backend">
    - `views/pos_anonymous_override_views.xml` defines `action_pos_anonymous_override` (`ir.actions.act_window`, view modes `list,form`, grouped by `create_date:day`) and a `point_of_sale.menu_point_rep` menu item `menu_pos_anonymous_override`.
    - `views/res_config_settings_views.xml` inherits `base.res_config_settings_view_form` to add the Ticketbooth Mode toggle under the POS settings section.
  </Tab>
  <Tab title="Templates & Data">
    - `data/mail_template.xml` — `email_template_pos_event_order_confirmation` (model `pos.order`).
    - `data/ir_cron.xml` — `ir_cron_pos_ticket_email_batches` (model `pos.ticket.email.batch`).
  </Tab>
  <Tab title="Inherited POS templates">
    The following `t-inherit` templates are registered in the manifest under `point_of_sale._assets_pos` or `point_of_sale.customer_display_assets`:
    - `pos_qol_overrides.ActionpadWidget` extends `point_of_sale.ActionpadWidget`.
    - `pos_qol_overrides.ProductScreen` extends `point_of_sale.ProductScreen`.
    - `pos_qol_overrides.Navbar` extends `point_of_sale.Navbar`.
    - `pos_qol_overrides.EventConfiguratorPopup` extends `pos_event.EventConfiguratorPopup`.
    - `pos_qol_overrides.EventConfiguratorPopupFooter` extends `pos_event.EventConfiguratorPopup`.
    - `pos_qol_overrides.PartnerList` and `pos_qol_overrides.PartnerLine` extend `point_of_sale.PartnerList` and `point_of_sale.PartnerLine`.
    - `pos_qol_overrides.Orderline` extends `point_of_sale.Orderline`.
    - `pos_qol_overrides.EventRegistrationPopup` extends `pos_event.EventRegistrationPopup`.
    - `pos_qol_overrides.ReceiptScreen` and `pos_qol_overrides.ReceiptScreenPresence` extend `point_of_sale.ReceiptScreen`.
    - `pos_qol_overrides.CustomerDisplay` extends `point_of_sale.CustomerDisplay`.
    - `pos_qol_overrides.LoginScreen` extends `point_of_sale.LoginScreen`.
  </Tab>
  <Tab title="New OWL templates">
    - `pos_qol_overrides.EventLookupPopup` — guest check-in lookup dialog.
    - `pos_qol_overrides.EventAttendeeListPopup` — event door list with search and grouped mark-as-present.
    - `pos_qol_overrides.TicketboothScanResult` — post-scan confirmation popup for ticketbooth mode.
    - `pos_qol_overrides.LoginScreen` — adds "Re-pair Card Reader" button for MAM POS kiosk app.
  </Tab>
  <Tab title="Public receipt templates">
    - `views/pos_receipt_templates.xml` — `pos_receipt_page` (public receipt view) and `pos_receipt_not_found` (error page).
  </Tab>
</Tabs>

# Business Logic

<Steps>
  <Step title="Session start">
    `pos_store.js` patches `PosStore.openOpeningControl()` to auto-confirm the opening control popup with zero cash and empty notes, bypassing the dialog.
  </Step>
  <Step title="Cart interaction">
    `order_summary.js` makes Backspace in quantity mode remove the selected orderline instead of decrementing quantity. `product_screen.js` removes `-`, `discount`, `price`, and decimal-point buttons from the numpad and adds a `payCash()` helper that sets `pos.payWithCash` and navigates to payment.
  </Step>
  <Step title="Payment screen">
    `payment_screen.js` consumes the `payWithCash` flag in `onMounted()` to pre-select the cash payment method and remove an empty auto-created payment line when present.
  </Step>
  <Step title="Event registration popup">
    `event_registration_popup.js` adds:
    - A `Specify Tickets` toggle that hides tickets 2..N by default and fills empty answers from ticket #1.
    - An `Override - Anonymous` panel that records a `pos.anonymous.override` with category and reason, then blanks and fills mandatory answers with defaults (`tickets@memphisartmuseum.org`, `9015446200`, or `-`).
    - A blocked-domain guard (`@memphisartmuseum.org`) that rejects confirm unless an anonymous override is active.
  </Step>
  <Step title="Order paid">
    `pos_order.py`:
    - `action_pos_order_paid()` sends an immediate `email_template_pos_event_order_confirmation` mail for event-ticket orders with a paid total and a registration email.
    - `send_paid_order_mail()` (called by `pos_event`'s `action_pos_order_paid()`) overrides the core per-registration ticket flow by creating batched `pos.ticket.email.batch` records (chunk size 25) and triggering the cron.
    - `event_registration.py` and `event_mail.py` suppress normal event mail schedulers for registrations that have a `pos_order_id`, so the order-level and batch-level emails do not duplicate.
    - `_enqueue_blackbaud_memberships()` is called for every paid order. If `mam_integration_hub` is installed, the order contains products under the "Membership" category, and the partner has an email, a `mam.blackbaud.sync.job` is queued with operation `sale`.
    - `_ensure_membership_portal_sms()` is called for every paid order. If the order contains membership products and the partner has email and phone, it calls `pos.payment.method.stripe_tap_ensure_portal_access()` to create a portal account and send credentials via SMS (rerouted to Mandrill when `mailchimp_x_odoo_txsms` is installed).
    - `_ensure_access_token()` is called for every paid order to generate a public receipt access token.
  </Step>
  <Step title="Ticket email batching">
    `pos_ticket_email_batch.py`:
    - `queue_order()` creates at most one set of batch records per `pos.order`, splitting registrations into 25-ticket chunks.
    - `_cron_process_pending_batches()` runs every 5 minutes, renders `event.action_report_event_registration_full_page_ticket` per registration, attaches PDFs, sends one mail per batch, and unlinks the attachments after send.
  </Step>
  <Step title="Guest check-in / presence">
    - `pos_order.py` exposes `action_mark_registrations_present()` (toggle all non-cancelled registrations between `draft` and `done`), `action_mark_group_present()` / `action_unmark_group_present()` (idempotent group mark), `lookup_tickets_by_partner()` (group a partner's tickets by event/day), and `lookup_registrations_by_reference()` (find an order by `pos_reference`).
    - `event_registration.py` exposes `lookup_attendees_by_event()` (door list grouped by identity) and `action_pos_ticketbooth_scan()` (barcode scan + mark present).
    - `event_lookup_popup.js` uses the order-reference methods for a `Guest Check-In` dialog.
    - `event_attendee_list_popup.js` uses `lookup_attendees_by_event` for a per-event door list.
    - `receipt_screen.js` adds a `Mark as Present` / `Here` button and auto-marks present when the event/slot start is within ±12 hours of now.
    - `barcode_reader_ticketbooth.js` intercepts scans when `pos.config.x_ticketbooth_mode` is enabled and calls `action_pos_ticketbooth_scan`.
  </Step>
  <Step title="Customer display">
    `customer_display_adapter.js` and `customer_display.xml` mirror the member-pricelist discount line from the orderline patch.
  </Step>
  <Step title="Public receipt">
    `controllers/pos_receipt.py` exposes `/pos/receipt/<int:order_id>` with an `access_token` query parameter. The controller renders `pos_receipt_page` (or `pos_receipt_not_found` if the order does not exist or the token is invalid). The receipt URL is computed by `_compute_access_url()` on `pos.order`.
  </Step>
</Steps>

<Info>
  Confidence: **high**. The logic is fully derived from the source files and comments.
</Info>

# Reports & QWeb

This module does not define new reports or paperformats. It reuses the native `event` report:

- `event.action_report_event_registration_full_page_ticket` — rendered inside `pos.ticket.email.batch._send_ticket_email()` to generate individual ticket PDFs.

It also defines two public-facing QWeb templates in `views/pos_receipt_templates.xml`:

- `pos_qol_overrides.pos_receipt_page` — styled receipt view showing company logo, order reference, date, cashier, customer, line items, subtotal/tax/total, and payment methods.
- `pos_qol_overrides.pos_receipt_not_found` — error page shown when the order does not exist or the access token is invalid.

<Warning>
  Name collision: the module uses `order_id` and `order_uuid` on both `pos.order` and `pos.anonymous.override`. Confidence lowered to **medium** only in cross-module contexts; within the module the fields are unambiguous.
</Warning>

# Controllers & Routes

| Route | Method | Auth | Purpose |
|-------|--------|------|---------|
| `/pos/receipt/<int:order_id>` | `GET` | `public` | Render a public receipt page for the given POS order. Gated by `access_token` matching `order.access_token`. |

The controller (`controllers/pos_receipt.py`) uses `request.render` with the templates defined in `views/pos_receipt_templates.xml`. No JSON/RPC endpoints are added.

# Security

| ID | Model | Group | Read | Write | Create | Delete |
|----|-------|-------|------|-------|--------|--------|
| `access_pos_anonymous_override_user` | `pos.anonymous.override` | `base.group_user` | 1 | 1 | 1 | 0 |
| `access_pos_ticket_email_batch_system` | `pos.ticket.email.batch` | `base.group_system` | 1 | 1 | 1 | 1 |

No record rules, implied groups, or dedicated groups are added by this module. The cron runs as `base.user_root`.

# Static Assets / OWL

## OWL component patches

| File | Patched component | Purpose |
|------|-------------------|---------|
| `static/src/app/services/pos_store.js` | `PosStore` | Skip opening control popup. |
| `static/src/app/screens/product_screen/order_summary/order_summary.js` | `OrderSummary` | Backspace removes line. |
| `static/src/app/screens/payment_screen/payment_screen.js` | `PaymentScreen` | Pre-select cash when `payWithCash` is set. |
| `static/src/app/screens/product_screen/product_screen.js` | `ProductScreen` | Simplified numpad, `payCash()` helper. |
| `static/src/app/screens/product_screen/action_pad/action_pad.js` | `ActionpadWidget` | Cash button on cart action pad. |
| `static/src/app/components/popup/event_configurator_popup/event_configurator_popup.js` | `EventConfiguratorPopup` | Pointer-based confirm/cancel to prevent iOS click-through. |
| `static/src/app/screens/partner_list/partner_list.js` | `PartnerList` / `PartnerLine` | Mobile full-screen customer dialog, touch-gesture selection. |
| `static/src/app/components/orderline/orderline.js` | `Orderline` | Swipe-to-delete and member-pricelist discount display. |
| `static/src/app/components/popup/event_registration_popup/event_registration_popup.js` | `EventRegistrationPopup` | Specify tickets, anonymous override, blocked-domain guard. |
| `static/src/app/components/popup/event_attendee_list_popup/event_attendee_list_popup.js` | `EventAttendeeListPopup` | Door list with search, grouped by identity, mark/unmark present. |
| `static/src/app/components/popup/ticketbooth_scan_result/ticketbooth_scan_result.js` | `TicketboothScanResult` | Post-scan confirmation for ticketbooth mode. |
| `static/src/app/services/barcode_reader_ticketbooth.js` | `BarcodeReader` (ticketbooth mode) | Intercepts barcode scans in ticketbooth mode and calls `action_pos_ticketbooth_scan`. |
| `static/src/app/screens/receipt_screen/receipt_screen.js` | `ReceiptScreen` | Email/phone pickers, mark-as-present, auto-mark near event start. |
| `static/src/app/customer_display/customer_display_adapter.js` | `CustomerDisplayPosAdapter` | Add member-pricelist discount data for customer display. |
| `static/src/app/navbar/navbar_guest_checkin.js` | `Navbar` | Add `Guest Check-In` menu entry. |

## New OWL components

| File | Component | Purpose |
|------|-----------|---------|
| `static/src/app/components/popup/event_lookup_popup/event_lookup_popup.js` | `EventLookupPopup` | Dialog to search order by reference and toggle presence. |
| `static/src/app/components/popup/event_attendee_list_popup/event_attendee_list_popup.js` | `EventAttendeeListPopup` | Dialog to list/search attendees for an event and mark/unmark groups. |
| `static/src/app/components/popup/ticketbooth_scan_result/ticketbooth_scan_result.js` | `TicketboothScanResult` | Dialog shown after a ticketbooth barcode scan. |

## Styles

- `static/src/app/screens/payment_screen/payment_screen.scss` — hides the `.js_invoice` button.
- `static/src/app/screens/partner_list/partner_list.scss` — makes the customer dialog full-screen on mobile/tablet.
- `static/src/app/components/orderline/orderline.scss` — swipe-to-delete transitions.
- `static/src/app/components/popup/event_attendee_list_popup/event_attendee_list_popup.scss` — attendee list styling and action buttons.
- `static/src/app/components/popup/ticketbooth_scan_result/ticketbooth_scan_result.scss` — ticketbooth scan result styling.
- `static/src/app/components/popup/partner_tickets_popup/partner_tickets_popup.scss` — partner ticket popup styling.
- `static/src/app/navbar/navbar_guest_checkin.xml` — navbar menu item for guest check-in.
- `static/src/app/components/popup/event_lookup_popup/event_lookup_popup.xml` — event lookup popup template.
- `static/src/app/components/popup/event_attendee_list_popup/event_attendee_list_popup.xml` — attendee list popup template.
- `static/src/app/components/popup/ticketbooth_scan_result/ticketbooth_scan_result.xml` — ticketbooth scan result template.
- `static/src/app/components/popup/partner_tickets_popup/partner_tickets_popup.xml` — partner tickets popup template.

## Asset bundles

- `point_of_sale._assets_pos` — all JS patches, OWL templates, and SCSS listed above.
- `point_of_sale.customer_display_assets` — `static/src/customer_display/customer_display.xml`.

# Troubleshooting

- **Immediate confirmation email not sent** — Verify the mail template `pos_qol_overrides.email_template_pos_event_order_confirmation` exists and the order has a paid total and at least one non-cancelled event registration with an email address.
- **Ticket batch emails stuck** — Ensure cron `pos_qol_overrides.ir_cron_pos_ticket_email_batches` is active and the mail server is configured. Check `pos.ticket.email.batch` records in `error` state for SMTP/report failures.
- **No PDF attachments** — The batch renderer relies on `event.action_report_event_registration_full_page_ticket`. If the `event` module or report is missing, batches will fail.
- **Cash button not pre-selecting** — `pos.payWithCash` is a one-shot flag set by `payCash()`. It must be consumed before `PaymentScreen.onMounted()` runs; otherwise payment methods are selected normally.
- **Swipe-to-delete not working** — Only enabled on small screens (`ui.isSmall`), `display` mode, and when line quantity changes are allowed (`!pos.disallowLineQuantityChange()`).
- **Blocked domain error** — Confirming an event registration with an email ending in `@memphisartmuseum.org` requires an active anonymous override signed by a cashier.
- **Ticketbooth mode not working** — Verify `pos.config.x_ticketbooth_mode` is enabled for the register. In ticketbooth mode, the barcode reader calls `event.registration.action_pos_ticketbooth_scan` instead of adding products to the cart.
- **Attendee list empty** — `EventAttendeeListPopup` only shows registrations linked to a `pos_order_id` for the selected event/slot. Verify tickets were sold through POS and are not cancelled.
- **Group mark/unmark fails** — The popup calls `pos.order.action_mark_group_present` / `action_unmark_group_present` with a list of `event.registration` IDs. The user must have write access on `event.registration`.
- **Blackbaud sync job not created** — `_enqueue_blackbaud_memberships()` requires `mam_integration_hub` to be installed, a product category named exactly "Membership" in the ancestor path, and a partner email. It is wrapped in try/except, so failures are logged silently.
- **Duplicate event emails still sent** — Both `event_registration._update_mail_schedulers()` and `event_mail._send_mail()` now suppress POS-linked registrations. If a third-party module also overrides `event.mail._send_mail`, verify suppression is not bypassed.
- **Membership portal SMS not sent** — `_ensure_membership_portal_sms()` requires `pos_stripe_tap` to be installed, a product category named "Membership" (case-insensitive), and both partner email and phone. It is wrapped in try/except, so failures are logged silently. The SMS is rerouted to Mandrill when `mailchimp_x_odoo_txsms` is installed.
- **Public receipt link invalid** — The receipt URL requires a valid `access_token` query parameter matching `order.access_token`. Tokens are generated by `_ensure_access_token()` during `action_pos_order_paid()`. If the order was created before this module was installed, it may not have a token.

<Warning>
  Confidence: **medium**. Troubleshooting notes are inferred from the code; runtime behavior may vary based on `pos_event` and `event` versions.
</Warning>

# Native Odoo 19 Comparison

### `EventRegistration._update_mail_schedulers`

**Native Odoo 19:** Runs the per-registration event mail scheduler for every `event.registration`, including those created from POS orders.

**This module:** Filters out registrations with `pos_order_id` and only calls `super()` on the remainder. POS tickets are delivered by the order confirmation and batch emailer instead.

**Why it matters:** Prevents duplicate event emails for POS-sold tickets while preserving scheduler behavior for other registration sources.

### `PosOrder.send_paid_order_mail`

**Native Odoo 19 (`pos_event`):** Calls `send_paid_order_mail()` and loops through each registration to send one ticket email per registration.

**This module:** Replaces the implementation without calling `super()`. It queues the entire order into `pos.ticket.email.batch` records and triggers the cron.

**Why it matters:** Ticket PDFs are batched (25 per mail) and sent asynchronously, reducing SMTP overhead and avoiding per-registration send latency during payment.

### `PosOrder.action_pos_order_paid`

**Native Odoo 19:** Does not send an immediate order confirmation for event ticket orders out of the box.

**This module:** After calling `super()`, sends `email_template_pos_event_order_confirmation` to the first non-empty registration email for paid orders that contain event tickets. Also calls `_enqueue_blackbaud_memberships()`, `_ensure_membership_portal_sms()`, and `_ensure_access_token()`.

**Why it matters:** Customers receive an immediate order confirmation with a reference number, while tickets follow later in the batch email. Membership sales are synced to Blackbaud and portal credentials are sent automatically.

### `PosStore.openOpeningControl`

**Native Odoo 19:** Displays the opening control popup asking for opening cash and notes.

**This module:** Auto-confirms with `0` cash and empty notes, skipping the dialog.

**Why it matters:** Speeds up front-desk session opening for a museum register that does not require a cash count at open.

### `OrderSummary.updateSelectedOrderline`

**Native Odoo 19:** Backspace in quantity mode decrements the line quantity to zero, requiring a second action or another confirmation step to remove the line.

**This module:** Removes the entire selected line immediately on Backspace when in quantity mode and quantity changes are allowed.

**Why it matters:** Fewer taps for cashiers removing mistaken items.

### `ProductScreen.getNumpadButtons`

**Native Odoo 19:** Exposes `-`, `discount`, `price`, and decimal-point buttons.

**This module:** Removes those buttons and reorders the remaining buttons so `Qty` sits immediately left of `Backspace`.

**Why it matters:** Simplifies the UI for a museum front desk that does not use discounts, manual prices, or negative quantities.

### `PaymentScreen.onMounted`

**Native Odoo 19:** Does not pre-select any payment method when arriving from the register.

**This module:** When `pos.payWithCash` is set, it clears any empty auto-created payment line and selects or creates the cash payment method line.

**Why it matters:** Allows a one-tap cash workflow from the mobile register.

### `Orderline` (swipe and pricelist discount)

**Native Odoo 19:** No swipe-to-delete gesture; no explicit member-pricelist strikethrough display on the orderline.

**This module:** Adds leftward swipe-to-delete on mobile, and computes a `pricelistDiscount` object comparing the pricelist unit price against the default product price.

**Why it matters:** Improves mobile cart interaction and makes active member discounts visible to the cashier without changing totals.

### `EventRegistrationPopup`

**Native Odoo 19:** Shows all ticket question forms immediately; no anonymous override workflow.

**This module:** Collapses tickets 2..N behind a `Specify Tickets` toggle, adds an `Override - Anonymous` panel with category/reason, and blocks `@memphisartmuseum.org` emails unless the override is signed.

**Why it matters:** Reduces form clutter for single ticket purchases and allows staff to override identity requirements for internal or privacy cases.

### `ReceiptScreen`

**Native Odoo 19:** Shows email/phone fields for receipt and prints badges for event registrations.

**This module:** Adds email/phone pickers populated from ticket answers, replaces the badge print flow with a `Mark as Present` / `Here` toggle, and auto-marks present when the event starts within ±12 hours.

**Why it matters:** Streamlines arrival tracking at the entrance; cashiers can pick contact information from the ticket instead of retyping it.

### `CustomerDisplay` / `CustomerDisplayPosAdapter`

**Native Odoo 19:** Shows the orderline without a member-pricelist discount line.

**This module:** Mirrors the `Orderline` patch on the customer-facing display by adding a strikethrough default price and member discount percentage.

**Why it matters:** Guests see the discount breakdown at the register.

### `EventLookupPopup` (new)

**Native Odoo 19:** No built-in order-reference lookup from the POS navbar for event registration check-in.

**This module:** Adds a `Guest Check-In` dialog that searches by `pos_reference`, lists attendees, and toggles presence.

**Why it matters:** Gives entrance staff a quick lookup without leaving the POS session.

### `PosReceiptController` (new)

**Native Odoo 19:** No public receipt page for POS orders.

**This module:** Adds a token-gated public receipt page at `/pos/receipt/<order_id>` with company branding, line items, and payment breakdown.

**Why it matters:** Customers can view and share their receipt without logging into a portal.

### `LoginScreen` (new)

**Native Odoo 19:** Standard login screen with cashier selection.

**This module:** Adds a "Re-pair Card Reader" button that calls `window.AndroidPos.repairReader()` when available (MAM POS kiosk app only).

**Why it matters:** Allows kiosk staff to re-pair Stripe card readers without restarting the POS session.

## See Also

- [`header_replacer`](/modules/header_replacer) — shares the
  `point_of_sale._assets_pos` bundle and patches the same `EventRegistrationPopup`;
  load order determines which setup/autofill behavior wins.
- [`mobile_pos_relayout`](/modules/mobile_pos_relayout) — shares the
  `point_of_sale._assets_pos` bundle and patches the same `Navbar`,
  `ProductScreen`, and `ActionpadWidget` templates.
- [`pos_stripe_tap`](/modules/pos_stripe_tap) — shares the
  `point_of_sale._assets_pos` bundle and provides the `ProductScreen.fastValidate`
  method called by this module's action-pad Card button. Also provides
  `stripe_tap_ensure_portal_access()` used for membership portal SMS.
- [`mam_membership_loyalty`](/modules/mam_membership_loyalty) — sets the member
  pricelist that this module displays as a discount line.
- [`website_event_calendar_slots`](/modules/website_event_calendar_slots) — the
  likely provider of the `event.slot` model read by the receipt screen's
  auto-mark-present logic.
- [`mam_setup`](/modules/mam_setup) — install-all meta module that does **not**
  currently list this module as a dependency; this module must be installed
  independently or added to `mam_setup`'s manifest.
- [`mailchimp_x_odoo_txsms`](/modules/mailchimp_x_odoo_txsms) — reroutes SMS
  sent by `_ensure_membership_portal_sms()` to Mandrill.

## Recommendations

<Warning>
  Confidence: **medium** — these are opinions, not verified fixes. Review before acting.
</Warning>

### High impact, low effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 1 | Fix `EventLookupPopup.toggleAll()` to pass the `pos.order` ID instead of `event.registration` IDs to `action_mark_registrations_present()`. | The lookup popup currently sends registration IDs as the recordset for `pos.order/action_mark_registrations_present`, which searches for registrations whose `pos_order_id` is in those IDs. The result is likely a no-op, so the **Toggle All** button in the guest lookup dialog does not actually mark anyone present. | Minimal code change; add `order_id` to the lookup result or keep it in the popup state. | high |
| 2 | Add `pos_qol_overrides` to the `mam_setup` install-all manifest `depends`. | `mam_setup` is the intended install-all meta module, but this POS module is not listed there. Without it, a fresh install from `mam_setup` will omit the POS quality-of-life features documented here. | One-line manifest change; no code behavior change. | high |
| 3 | Extract the duplicated member-pricelist discount calculation into a shared utility or OWL mixin. | The same logic (`getPrice(null, qty, priceExtra, false, product)`, percent math, `formatCurrency`) appears in `orderline.js` and `customer_display_adapter.js`. Keeping them in sync is error-prone if either gets adjusted. | Small refactor; both files import the helper, reducing drift risk. | high |
| 4 | Move the hard-coded `@memphisartmuseum.org` blocked domain out of the JS patch and into an `ir.config_parameter` or model field. | Museum staff or domain changes should not require a frontend rebuild. A configurable value is easier to maintain across staging and production. | Adds one RPC/config read at popup setup; trivial administrative change. | medium |

### High impact, high effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 1 | Introduce explicit access rights and record rules for `pos.anonymous.override`. | Currently any `base.group_user` member can read and create every override record. In a multi-cashier environment this could leak reasons/names across sessions. | Requires deciding ownership model (session, user, company). Adds migration for existing records. | medium |
| 2 | Replace the custom `pos.ticket.email.batch` cron batcher with a native Odoo 19 mail-queue approach or consolidate it into a server-side queue job. | The module overrides `pos_event.send_paid_order_mail()` without calling `super()`, which makes the customization fragile if `pos_event` changes. A queue-based or mail-template-driven approach would be more idiomatic and easier to monitor. | Significant rewrite; must preserve 25-ticket PDF chunking and immediate confirmation email separation. | medium |

### Low impact, low effort

| # | Recommendation | Reasoning | Trade-off | Confidence |
|---|----------------|-----------|-----------|------------|
| 1 | Make the ±12-hour auto-mark-present window on `ReceiptScreen` configurable. | The window is currently hard-coded in `receipt_screen.js`. Different events or shifts may want a different threshold. | Adds a small config parameter; no functional change if default is kept. | low |
| 2 | Add `website_event_calendar_slots` to the manifest `depends` or guard `event_slot_id.start_datetime` usage with a fallback. | The `event.slot` model is not declared as a dependency and is only available when the companion module is installed. The code already falls back to `event_id.date_begin`, but the soft dependency is undocumented in the manifest. | One-line manifest change or defensive check; may introduce an unwanted hard dependency if the slot module is optional. | medium |
| 3 | Add `mailchimp_x_odoo_txsms` to the manifest `depends` or document the soft dependency. | `_ensure_membership_portal_sms()` sends SMS that is rerouted to Mandrill when this module is installed, but the dependency is not declared. If the module is missing, SMS falls back to the default Odoo SMS gateway. | One-line manifest change or documentation update; no functional change. | medium |

### Odoo-native alternatives

| Custom pattern | Native Odoo 19 equivalent | Confidence |
|----------------|---------------------------|------------|
| `pos.ticket.email.batch` cron batcher | Native Odoo 19 mail template + `mail.mail` queue with `auto_delete` already does per-mail queueing. The batching here is custom to avoid 25 separate SMTP transactions and PDF renders per order. | medium |
| `pos.anonymous.override` model | Could be replaced with an `event.registration` state/note or a `res.partner` placeholder record, but the audit trail (category, reason, signed-by) is intentionally custom. | low |
| `EventLookupPopup` guest lookup | Could be built as a backend wizard (`TransientModel`) that returns an action, rather than an RPC from the POS navbar. | low |
| Public receipt page (`PosReceiptController`) | Could be replaced with a customer portal page using Odoo's `portal.mixin` framework, but the token-gated lightweight receipt is intentionally standalone. | low |

# Agent Review Log

<AccordionGroup>
  <Accordion title="2026-08-30 — Engine:Scribe">
    - Updated `pos_qol_overrides` after diff review of changes since 2026-08-10.
    - Added `login_screen_repair.js` / `login_screen_repair.xml` documentation for card reader re-pair button.
    - Updated `generation_date` to 2026-08-30.
  </Accordion>
  <Accordion title="2026-08-10 — Engine:Scribe">
    - Updated `pos_qol_overrides` after diff review of changes since 2026-08-09.
    - Added `pos_stripe_tap` to direct manifest dependencies (new in manifest).
    - Documented new controller `PosReceiptController` with public receipt route `/pos/receipt/<order_id>`.
    - Documented new methods on `pos.order`: `_ensure_membership_portal_sms()`, `_ensure_access_token()`, `_compute_access_url()`.
    - Documented new receipt templates in `views/pos_receipt_templates.xml`.
    - Added `mailchimp_x_odoo_txsms` as soft dependency for Mandrill SMS rerouting.
    - Updated `generation_date` to 2026-08-10.
  </Accordion>
  <Accordion title="2026-08-09 — Engine:Scribe">
    - Regenerated page after reviewing the current module source.
    - Documented new models: `pos.config.x_ticketbooth_mode`, `res.config.settings.pos_x_ticketbooth_mode`, `event.mail._send_mail` override.
    - Documented new backend methods: `action_mark_group_present`, `action_unmark_group_present`, `lookup_tickets_by_partner`, `lookup_attendees_by_event`, `action_pos_ticketbooth_scan`, `_enqueue_blackbaud_memberships`.
    - Documented new frontend components: `EventAttendeeListPopup`, `TicketboothScanResult`, ticketbooth barcode reader patch.
    - Refreshed `generation_date` to 2026-08-09.
  </Accordion>
  <Accordion title="2026-08-10 — Engine:Cartographer">
    - Verified `pos_stripe_tap` is now a direct manifest dependency (added since 2026-08-09).
    - Verified `_ensure_membership_portal_sms()` calls `pos.payment.method.stripe_tap_ensure_portal_access()` from `pos_stripe_tap`; confirmed in `pos_stripe_tap/models/pos_payment_method.py`.
    - Verified `mailchimp_x_odoo_txsms` reroutes all `sms.sms` through Mandrill by overriding `_split_by_api()`; this affects SMS sent by `_ensure_membership_portal_sms()`.
    - Added `mailchimp_x_odoo_txsms` to soft dependencies and `See Also`.
    - Verified `PosReceiptController` and receipt templates are self-contained within `pos_qol_overrides`; no other module references them.
  </Accordion>
  <Accordion title="2026-08-09 — Engine:Cartographer">
    - Added the Blackbaud connection: `pos.order._enqueue_blackbaud_memberships()` calls `mam_integration_hub.mam.blackbaud.source.enqueue_membership_sync()`.
    - Noted that `pos_qol_overrides` still does not extend or inherit from other MAM modules except via optional runtime checks.
  </Accordion>
  <Accordion title="2026-08-10 — Engine:Continuity">
    - Verified `pos_stripe_tap` is now a direct manifest dependency (was soft dependency as of 2026-08-05). Updated `See Also` and dependency tables accordingly.
    - Verified `mailchimp_x_odoo_txsms` is a new soft dependency for SMS rerouting; added cross-reference.
    - Verified public receipt controller (`PosReceiptController`) is self-contained; no cross-module references.
    - No new contradictions or naming collisions introduced by the 2026-08-10 diff.
    - `last_reviewed_date` remains 2026-08-09; no structural corrections required.
  </Accordion>
  <Accordion title="2026-08-09 — Engine:Continuity">
    - Updated `See Also` to include `mam_integration_hub`.
    - Confirmed no contradictions with the rest of the docset.
  </Accordion>
  <Accordion title="2026-08-10 — Engine:Advisor">
    - Reviewed new code since 2026-08-09: `pos_receipt.py` controller, receipt templates, `_ensure_membership_portal_sms()`, `_ensure_access_token()`.
    - Added recommendation to add `mailchimp_x_odoo_txsms` to manifest or document the soft dependency.
    - Added recommendation to consider Odoo portal mixin for public receipt instead of custom controller.
    - Existing recommendations (EventLookupPopup toggle bug, mam_setup dependency, discount utility extraction, blocked domain config) remain valid.
  </Accordion>
  <Accordion title="2026-08-09 — Engine:Advisor">
    - Reviewed existing recommendations; the `mam_setup` dependency recommendation remains valid.
    - No new high-priority recommendations added beyond existing ticketbooth and attendee-list documentation.
  </Accordion>
  <Accordion title="2026-08-02 — Engine:Scribe">
    - Initial generation of the `pos_qol_overrides` developer-reference page.
    - Documented two new models (`pos.anonymous.override`, `pos.ticket.email.batch`) and two inherited models (`event.registration`, `pos.order`).
    - Documented all POS frontend patches, SCSS overrides, the guest check-in popup, and the ticket email batching flow.
    - Added native Odoo 19 comparison entries for the major overrides.
    - Overall confidence set to **medium** because troubleshooting and native-behavior claims are inferred from code and comments rather than verified at runtime.
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Cartographer">
    - Added `Cross-Module Connections` section after `Dependencies`.
    - Set `confidence_map.cross_module_connections` to **medium** based on static analysis of manifests and source files.
    - Verified that no other custom module inherits from or references models, XML IDs, or methods introduced by `pos_qol_overrides` (`pos.anonymous.override`, `pos.ticket.email.batch`, `pos.order.send_paid_order_mail`, `action_mark_registrations_present`, `lookup_registrations_by_reference`).
    - Documented shared `point_of_sale._assets_pos` and `point_of_sale.customer_display_assets` bundles with `header_replacer`, `mobile_pos_relayout`, and `pos_stripe_tap`.
    - Flagged `EventRegistrationPopup` as patched by both `pos_qol_overrides` and `header_replacer`; both modules mutate the same `state` fields and load order matters.
    - Flagged `Navbar`, `ProductScreen`, and `ActionpadWidget` as patched by both `pos_qol_overrides` and `mobile_pos_relayout`; XPath targets differ but the same base templates are assumed.
    - Flagged the member-pricelist discount display as a soft concern shared with `mam_membership_loyalty`, which provides the actual pricelist assignment.
    - No orphaned or unresolved references were found within this module.
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Continuity">
    - Added `continuity_review: "medium"` to `confidence_map`.
    - Added a `See Also` section with cross-references to `header_replacer`, `mobile_pos_relayout`, `pos_stripe_tap`, `mam_membership_loyalty`, `website_event_calendar_slots`, and `mam_setup`.
    - Documented the soft dependency on the `event.slot` model (read via `event_slot_id` in `receipt_screen.js`); the model is not defined in this repository and is typically supplied by a companion module.
    - Noted that `mam_setup` does not currently declare this module as a dependency.
    - Updated `last_reviewed_date` and `reviewed_by` to reflect this continuity pass.
    - Kept overall confidence at **medium** (static analysis only, untested at runtime).
  </Accordion>
  <Accordion title="2026-08-05 — Engine:Advisor">
    - Added **Recommendations** section with `confidence_map.recommendations` set to **medium**.
    - Flagged a likely bug in `EventLookupPopup.toggleAll()`: it passes `event.registration` IDs to `pos.order/action_mark_registrations_present`, which expects `pos.order` IDs.
    - Recommended adding `pos_qol_overrides` to the `mam_setup` manifest dependency list.
    - Recommended extracting duplicated member-pricelist discount calculation from `orderline.js` and `customer_display_adapter.js`.
    - Recommended making the blocked email domain and the auto-mark-present window configurable.
    - Recommended stronger ACLs and record rules for `pos.anonymous.override`.
    - Noted the soft dependency on `website_event_calendar_slots` for the `event_slot_id` field.
  </Accordion>
</AccordionGroup>
