---
title: "Continuity Report"
description: "Global documentation health and cross-module inconsistencies."
icon: "clipboard-check"
generated_by: "Engine:Continuity"
generation_date: "2026-09-06"
---

# Continuity Report

<Warning>
  This report was generated automatically. Items marked **unsure** need human review.
</Warning>

## Summary

| Metric | Count |
|--------|-------|
| Modules reviewed | 16 |
| Redundancies fixed | 0 |
| Contradictions flagged | 0 new |
| Naming collisions found | 0 new |
| Confidence adjustments | 0 |
| Cross-references added | 5 |
| Stale references removed | 0 |

## Open issues

| Module | Section | Issue | Confidence |
|--------|---------|-------|------------|
| `mam_membership_cards` | Business Logic | `_push_to_google_wallet` is a placeholder; Google Wallet integration not yet implemented. | medium |
| `event_mass_publish` | Cross-Module Connections | Reads `event.event.product_id`, `event.event.ticket.is_published`, and `product.template.event_id` from `header_replacer` but does not declare `header_replacer` in `__manifest__.py`. Works only when `mam_setup` installs both. | medium |
| `pos_event_visibility` | Dependencies / Cross-Module | Reads `product.template.event_id` and `event.event.ticket.is_published` from `header_replacer` but does not declare `header_replacer` in `__manifest__.py`. | medium |
| `website_event_photo_cover` | Dependencies | Reads `event.event.image_1024` introduced by `header_replacer` but does not declare `header_replacer` in `__manifest__.py`. | medium |
| `website_event_calendar_slots` | Cross-Module Connections | References `event.slot` model and `_filter_open_slots()` method, which are not defined in any custom module in this repository. Requires a companion module not documented here. | medium |
| `pos_qol_overrides` | Cross-Module Connections | `receipt_screen.js` reads `event.registration.event_slot_id.start_datetime` from the same external `event.slot` provider. The model is not defined in any custom module in this repository. | medium |
| `web_foldering_architecture` | Models / Security | `website.header.relay.queue` lacks an ACL row in `security/ir.model.access.csv`; non-superuser access is denied. | medium |
| `web_foldering_architecture` / `header_replacer` | Models | Hard-coded `website.group` origin ref `1111` and hard-coded view IDs `(1183, 2897)` are database-specific. | medium |
| `event_microsite` | Dependencies | `event_product` is not in manifest `depends` but `product.template` overrides are tightly coupled to it. | medium |
| `website_manipulator` | Security | Craft CMS credentials stored in plain text; `password="True"` only masks UI. | high |
| `header_replacer` | Security | ACL updated to include `base.group_public` read access (2026-08-30 diff). | high |

## Recently resolved

| Module | Issue | Resolution |
|--------|-------|------------|
| `pos_qol_overrides` / `pos_stripe_tap` | `pos_stripe_tap` was a soft dependency; now direct in manifest. | Updated `pos_qol_overrides` docs to reflect direct dependency. Added `pos_stripe_tap` to manifest `depends`. |
| `pos_qol_overrides` / `mailchimp_x_odoo_txsms` | SMS rerouting via Mandrill was undocumented. | Added `mailchimp_x_odoo_txsms` as soft dependency and cross-reference in `pos_qol_overrides` docs. |
| `header_replacer` / `pos_qol_overrides` | `EventRegistrationPopup` setup-patch conflict was documented only on the `pos_qol_overrides` page. | Added reciprocal POS bundle and `EventRegistrationPopup` conflict warning to `header_replacer`. Extended `See Also` on both pages. |
| `mobile_pos_relayout` | Page lacked a `See Also` section despite being referenced by other POS modules. | Added `See Also` with cross-references to `header_replacer`, `pos_qol_overrides`, `pos_stripe_tap`, `pos_event_visibility`, and `mam_setup`. |
| `pos_qol_overrides` | Missing `See Also` section and no `confidence_map.continuity_review` key. | Added `continuity_review: "medium"`, `See Also` section, and documented the `event_slot_id` soft dependency. |
| `pos_stripe_tap` | Missing `See Also` section and no `confidence_map.continuity_review` key. | Added `continuity_review: "medium"` and `See Also` section with cross-references to `pos_qol_overrides`, `mobile_pos_relayout`, `header_replacer`, and `mam_setup`. |
| `docs.json` | `pos_qol_overrides` and `pos_stripe_tap` were missing from the Modules navigation group even though their pages exist. | Added both entries alphabetically after `pos_event_visibility`. |
| `website_event_calendar_slots` | No reciprocal link to the module that also reads `event_slot_id`. | Added `See Also` link to `pos_qol_overrides` and noted the shared `event.slot` dependency. |
| `mam_membership_loyalty` | No reciprocal link to the module that displays its benefit pricelist. | Added `See Also` link to `pos_qol_overrides` and noted the member-pricelist discount display coupling. |

## Notes

- Full source-tree grep confirmed that `custom_product_tags` is referenced only by `mam_setup` (install-only reverse dependency) and by its own files. It was not part of this review pass.
- The `header_replacer` soft-dependency cluster (`event_mass_publish`, `pos_event_visibility`, `website_event_photo_cover`) is mitigated in production by installing `mam_setup`, but the individual manifests remain technically incomplete.
- The two new POS modules (`pos_qol_overrides`, `pos_stripe_tap`) are **not** listed in `mam_setup`'s manifest dependencies. The `mam_setup` docs accurately reflect the manifest, so this is currently a system-design observation rather than a documentation contradiction. If the install-all meta module is intended to pull the entire stack, the manifest should be updated.
- No new naming collisions were introduced by this continuity pass; existing field/method name collisions documented by earlier agents were preserved and cross-linked.
- The `mam_setup` install-all meta module remains the primary mechanism that guarantees the full stack is loaded together, but it now excludes the two newest POS modules unless they are added.
